import styled from "styled-components";

export const HomePageBase = styled.div`
    display: flex;
    flex-direction: row;
    width: 100vw;
    flex-wrap: wrap;

    main {
        margin: 10px 0;
        width: 100vw;
        display: flex;
        gap: 15px;
    }
`;
